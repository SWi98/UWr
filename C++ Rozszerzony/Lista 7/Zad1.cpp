#include <iostream>
#include <complex>
#include <cmath>
using namespace std;

complex<double> EulerGamma(complex<double> point, int iterations){
    return complex<double>(2.0, 10.0);
}

int main(){
    complex<double> test(2.0, 10.0);
    cout << test << endl;
    cout << arg(test) << endl;
    //cout << test / 1 << endl;
}